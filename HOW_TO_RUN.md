# Fiscal-Sentinel: Complete Setup & Run Guide

## 🚀 Quick Start (3 Steps)

### Step 1: Generate Sample Data
```bash
python create_dummy_data.py
```

This creates all test files in the `data/` folder.

### Step 2: Start the Backend
```bash
run_backend.bat
```

OR manually:
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Backend will run at: http://localhost:8000  
API Docs at: http://localhost:8000/docs

### Step 3: Start the Frontend
```bash
run_frontend.bat
```

OR manually:
```bash
streamlit run dashboard.py
```

Dashboard will open at: http://localhost:8501

---

## 📁 Generated Test Files

After running `create_dummy_data.py`:

- `data/tenders/bid_A.pdf` - First tender document
- `data/tenders/bid_B.pdf` - Second tender document (95% identical)
- `data/invoices/invoice_sample.png` - Invoice with inflated price
- `data/payroll/payroll.csv` - Payroll with ghost employees
- `data/welfare/pension_list.csv` - Pension beneficiaries
- `data/welfare/death_registry.csv` - Death registry

---

## 🧪 How to Test Each Module

### Module A: Tender-Watch
1. Go to "Procurement" tab
2. Upload both `bid_A.pdf` and `bid_B.pdf`
3. Click "Analyze Tenders"
4. **Expected:** 95%+ similarity detected

### Module B: Price-Guard
1. Go to "Spending" tab
2. Upload `invoice_sample.png`
3. Click "Scan Invoice"
4. **Expected:** Laptop flagged as overpriced (₹1,50,000 vs ₹80,000)

### Module C: Ghost-Hunter
1. Go to "Payroll" tab
2. Upload `payroll.csv`
3. Click "Scan Payroll"
4. **Expected:** 3 ghost employees detected

### Module D: Welfare-Shield
1. Go to "Welfare" tab
2. Upload `pension_list.csv` and `death_registry.csv`
3. Click "Cross-Check Beneficiaries"
4. **Expected:** 2 deceased beneficiaries found

---

## 🛠️ Dependencies

All dependencies are in `requirements.txt`:
- fastapi & uvicorn (Backend API)
- streamlit (Frontend Dashboard)
- sentence-transformers (AI for tender analysis)
- pytesseract (OCR for invoices)
- networkx (Graph analysis for payroll)
- rapidfuzz (Fuzzy matching for welfare)
- pdfplumber (PDF parsing)
- fpdf (PDF generation)
- pandas (Data handling)

---

## 🎯 Project Structure

```
Fiscal-Sentinel/
├── app/
│   ├── main.py              # FastAPI backend
│   └── modules/
│       ├── tender.py        # Module A
│       ├── price.py         # Module B
│       ├── ghost.py         # Module C
│       └── welfare.py       # Module D
├── data/                    # Generated by create_dummy_data.py
│   ├── tenders/
│   ├── invoices/
│   ├── payroll/
│   └── welfare/
├── create_dummy_data.py     # Data generator
├── dashboard.py             # Streamlit frontend
├── requirements.txt         # Dependencies
├── run_backend.bat          # Start API
└── run_frontend.bat         # Start UI
```

---

## ✅ Success Checklist

- [ ] Run `create_dummy_data.py` successfully
- [ ] Backend starts without errors at http://localhost:8000
- [ ] Frontend opens at http://localhost:8501
- [ ] All 4 modules detect fraud in test data
- [ ] Ready to demo!

---

## 🐛 Troubleshooting

**Issue:** "Module not found" errors  
**Solution:** Run `pip install -r requirements.txt`

**Issue:** PyTesseract not found  
**Solution:** Install Tesseract OCR from https://github.com/UB-Mannheim/tesseract/wiki

**Issue:** Backend connection error in dashboard  
**Solution:** Make sure backend is running (`run_backend.bat`)

---

## 🏆 You're Ready!

All code is complete and tested. Just run the 3 steps above and you're ready to demo!
